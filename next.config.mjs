/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com', 'avatars.githubusercontent.com'], // Add more domains if needed
    },
  };
  
  export default nextConfig;
  